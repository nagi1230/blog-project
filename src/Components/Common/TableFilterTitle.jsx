import React from 'react'

function TableFilterTittle({ title }) {

    return (
        <div>
            <h3 className="title-filter">{title}</h3>
        </div>
    )
}

export default TableFilterTittle
